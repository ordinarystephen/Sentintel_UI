/**
 * Workpaper-configuration vocabulary — DELIBERATE PLACEHOLDERS (refinement
 * round, 2026-09-18): the SHAPE (template select, section chips, custom
 * topics) is the design; the words in these lists are not settled. The
 * chosen values ride the review's evidence snapshot even while
 * placeholders.
 */
export const WORKPAPER_TEMPLATES = [
  'IB Lending — standard workpaper',
  'Placeholder — template B',
] as const
