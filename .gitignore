node_modules/
dist/
.DS_Store
test-results/
playwright-report/
e2e/screenshots/
.vite/
*.local
__pycache__/
.pytest_cache/

# local scratch drops (not project files)
Claude outputs/

# Pinned workspace proxy path — one line, per workspace session. A pin holds the
# pasted workspace URL (an internal host), so no pin may ever become
# committable: the glob also covers a pin written under the pre-v1.8.1 name.
/.*-workspace-path
